/**
 * Merge into your Retail React App Page Designer component registry.
 *
 * Example (app/page-designer/index.js):
 *
 * import {registerAmpliencePageDesignerComponents} from '@royalcyber/amplience-react';
 * import existingComponents from './components';
 *
 * const amplienceComponents = registerAmpliencePageDesignerComponents();
 * export default {...existingComponents, ...amplienceComponents};
 */
import {registerAmpliencePageDesignerComponents} from '@royalcyber/amplience-react';

export default registerAmpliencePageDesignerComponents();
